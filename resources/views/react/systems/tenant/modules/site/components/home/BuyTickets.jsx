import * as React from "react";
import Box from "@mui/material/Box";
import Grid from "@mui/material/Grid";

export default function BuyTickets(props) {
    return (
        <Box component="section" sx={{ backgroundColor: props.color, height: "25rem" }}>
            <Grid container></Grid>
        </Box>
    );
}
